// If your frontend is on GitHub Pages and your API/server is deployed elsewhere,
// set the backend URL here, for example:
// window.THREE_DO_API_URL = "https://your-backend.onrender.com";
// Leave it empty when frontend and backend are served from the same domain.
window.THREE_DO_API_URL = "";
